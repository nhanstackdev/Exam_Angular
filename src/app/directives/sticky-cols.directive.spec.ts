import { StickyColsDirective } from './sticky-cols.directive';

describe('StickyColsDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyColsDirective();
    expect(directive).toBeTruthy();
  });
});
